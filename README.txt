Dry-run leaves this folder empty of sends. That is the Part 4 proof:
nothing is written here unless a human approves a send (python demo.py --cap R3 --live).

Hostile addresses that must never appear in a file here:
  archive@mail-backup-service.info
  finance-sync@ext-audit.co
